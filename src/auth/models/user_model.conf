[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[role_definition]
g = _, _          # <- esta linha registra a função g

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = (g(r.sub.role, p.sub) && r.act == p.act && r.obj.role != "admin") \
    || r.sub.id == r.obj.id
